[project]
name = "ptf-prices"
version = "0.1.0"
description = "Price/FX service for ptf-engine (yfinance -> DuckDB -> Parquet)"
requires-python = ">=3.11,<3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "yfinance>=0.2.40",
    "duckdb>=1.0",
    "pandas>=2.2",
]
